console.clear();

require('dotenv').config({path: '.env'});
const { curly } = require('node-libcurl');
const urlHandler = require('../../handlers/url.handler');
const httpHeaderJson = ['Content-Type: application/json','Accept: application/json'];


describe('Connections [Watchtower]',  () => {

    test('Should be able to connect', async () => {

  
    });

    test('Should be able to disconnect', async () => {

  
    });

    test('Should be able to get services available', async () => {

  
    });
});

describe('Subscribe Topics [Watchtower]',  () => {

    test('Should be able to connect', async () => {

  
    });

    test('Should be able to disconnect', async () => {

  
    });
});

